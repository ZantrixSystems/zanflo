# Frontend environment — production/staging build
# Used when running: npm run build (without .env.local override)
# These values are baked into the JS bundle at build time.

VITE_API_URL=https://zanflow.zantrixsystems.workers.dev
VITE_TENANT_SLUG=riverside
